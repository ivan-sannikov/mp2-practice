#include <gtest.h>
#include "ScanTable.h"

TEST(ScanTable, Insert_ScanTable){
    ScanTable<int, int> a;
    //  a.Insert(1, 1);
   // TabRecord<int, int> b(1,1);
   // EXPECT_EQ(b.pData, a.Find(1)->pData);
}
